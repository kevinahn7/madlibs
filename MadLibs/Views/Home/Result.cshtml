<!DOCTYPE html>
<html>
<head>
    <title>Hello Friend!Hello Friend!</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
</head>
<body>
    <h1>The Greatest Adventure, starring @Model.GetName()</h1>
    <p>My name's @Model.GetName()</p>
    <p>I am @Model.GetAdjOne() and I like to @Model.GetVerbOne().</p>
    <p>When @Model.GetNounOne() @Model.GetVerbTwo() @Model.GetNounTwo(),</p>
    <p>I like to @Model.GetVerbThree() @Model.GetNounThree() @Model.GetAdverb().</p>

    <p>Then the @Model.GetAdjOne() @Model.GetNounOne() robbed the bank, but the @Model.GetNounOne() were spoiled.</p>
    <p>All thanks due to the @Model.GetAdverb() @Model.GetAdjOne() @Model.GetName()</p>

    <a href="/"><button class="btn btn-primary">Play Again</button></a>
</body>
</html>
